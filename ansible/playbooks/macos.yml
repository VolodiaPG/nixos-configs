---
- name: Configure macOS hosts
  hosts: darwin
  gather_facts: true
  roles:
    - nix
    - homebrew
    # After homebrew: roles/syncthing runs the Homebrew syncthing binary.
    - ssh_agent
    - syncthing
    - pkgs
    - shell
    - darwin_defaults
    - secrets
    - chezmoi
