---
# secrets: writes user-scope secrets from the ansible-vault (PLAN.MD task
# 5.2) to their existing dotfile paths — currently just ~/.envvars.sh, see
# defaults/main.yml for the full variable contract and why the other two
# original candidates were dropped.
dependencies: []
