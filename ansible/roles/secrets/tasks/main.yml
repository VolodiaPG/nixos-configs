---
# secrets: vault-sourced files into place
# Scaffold only - populated in a later phase. See PLAN.MD.
- name: "Placeholder | secrets role is not implemented yet"
  ansible.builtin.debug:
    msg: "role 'secrets' is a scaffold; see PLAN.MD"
