---
# base: locale fr_FR, timezone Europe/Paris, sysctl, sudo, sshd
# Scaffold only - populated in a later phase. See PLAN.MD.
- name: "Placeholder | base role is not implemented yet"
  ansible.builtin.debug:
    msg: "role 'base' is a scaffold; see PLAN.MD"
