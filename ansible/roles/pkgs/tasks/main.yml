---
# pkgs: platform-dispatching package role (dnf/flatpak/brew/nix)
# Scaffold only - populated in a later phase. See PLAN.MD.
- name: "Placeholder | pkgs role is not implemented yet"
  ansible.builtin.debug:
    msg: "role 'pkgs' is a scaffold; see PLAN.MD"
